#!/bin/sh

alias cfg='git --git-dir=$HOME/.dotcfg --work-tree=$HOME'
git clone --bare git@github.com:jjsullivan5196/dotfiles.git $HOME/.dotcfg
cfg checkout
cfg config --local status.showUntrackedFiles no
